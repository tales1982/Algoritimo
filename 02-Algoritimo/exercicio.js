function cliente() {
  let nome = prompt("Digite o nome do fucionario.");
  let nacimento = prompt("Digite o ano de naçimento do fucionario.");
  let salario = prompt("Digite o salario do fucionario.");
  alert(`-------FICHA FUNCIONAL-------\n
    NOME: ${nome}\n
    NAÇIMENTO: ${nacimento}\n
    SALARIO: ${salario}`);
}
cliente();
